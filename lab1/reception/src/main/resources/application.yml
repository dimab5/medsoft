server:
  port: 8082
  servlet:
    context-path: /api
  ssl:
    key-store: classpath:keystore/reception-keystore.jks
    key-store-password: password
    key-alias: reception-api
    key-password: password

spring:
  application:
    name: reception-service

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      properties:
        spring.json.adda.type.headers: false
        spring.json.trusted.packages: "*"
        spring.json.type.mapping: >-
          patientDto:com.reception.models.PatientDto
          
#keytool -genkeypair -alias localhost -keyalg RSA -keysize 2048 -validity 3650 -keystore localhost.jks -storetype JKS -dname "CN=localhost, OU=Development, O=MyCompany, L=Moscow, ST=Moscow, C=RU" -storepass changeit -keypass changeit